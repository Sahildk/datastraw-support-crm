{
  "name": "datastraw-support-crm-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "api": "venv\\Scripts\\uvicorn backend.main:app --reload"
  }
}
